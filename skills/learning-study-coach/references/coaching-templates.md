# 陪学模板

## 1. 日计划解析模板

```markdown
### 今日任务卡
- 周目标：
- 今日学习内容：
- 今日实操任务：
- 今日产出物：
- 今日验收标准：
- 学习材料：

### 初学者注意
- 今天第一次接触的新词：
- 教程默认省略但需要补充的前置知识：
- 最容易卡住的地方：

### 今天先完成什么
1) 必做：
2) 可选：
3) 时间不足时可延后：
```

## 2. 教程/课程任务卡

```markdown
### 当前教程任务卡
- 教程/课程名称：
- 当前章节 / Lesson / Module：
- 本节目标：
- 前置条件：
- 当前材料类型：文档 / 视频 / 代码仓库 / 幻灯片 / 混合
- 本节实操任务：
- 本节完成标志：
- 当前卡点：
- 本节第一次出现的新词：
- 教程隐含但没讲清的前置知识：
```

## 3. 讲解档位模板

```markdown
### 当前讲解档位
- 档位：零基础档 / 夯实基础档 / 冲刺复习档
- 为什么用这个档位：
- 如果太快或太慢，下一步如何调整：
```

## 4. 零基础预热模板

```markdown
### 开始前先讲清楚
- 这一步要解决什么问题：
- 用大白话解释：
- 你会第一次接触到的新词：
- 这些词在这一步分别起什么作用：
- 教程没写但你最好先知道的前置知识：
```

## 5. 单步辅导模板

```markdown
### Step N - [本步名称]

#### 1) 当前讲解档位
[零基础档 / 夯实基础档 / 冲刺复习档]

#### 2) 当前所在位置
[计划项 / 章节 / Lesson / Module / Step]

#### 3) 本步目标
[一句话说明本步要达成什么]

#### 4) 对应档位的讲解
- 零基础档：[不用术语，先解释这一步到底在做什么]
- 夯实基础档：[说明这一步和相关概念的关系]
- 冲刺复习档：[说明本步重点、风险点和检查点]

#### 5) 新词和隐藏前置知识
- 新词 1：
- 新词 2：
- 隐含前置知识：

#### 6) 为什么先做这步
[和当前学习目标或验收标准的关系]

#### 7) 技术细节说明
- 概念：
- 工具/命令作用：
- 关键参数解释：
- 目录或文件会发生什么变化：
- 预期结果：

#### 8) 现在请你执行
[命令1]
[命令2]
[命令3]

#### 9) 成功信号
- 预期输出：
- 验收点：

#### 10) 失败先排查
- 检查项 A：
- 检查项 B：
- 最小诊断动作：

#### 11) 完成后回传
- 请贴出：
- 如果你理解了，也请用一句话复述你觉得这一步在做什么：
```

## 6. Lesson / Module 陪学模板

```markdown
### Lesson N - [章节名]

#### 1) 这节在整个教程里的位置
[前一节学了什么，这一节解决什么问题]

#### 2) 这节只先掌握什么
- []
- []

#### 3) 当前讲解档位
- 档位：
- 原因：

#### 4) 初学者先知道什么
- 这节第一次出现的新词：
- 这节默认略过的前置知识：
- 最容易误解的点：

#### 5) 关键概念和命令解释
- 概念：
- 工具/命令：
- 关键参数：
- 目录/文件变化：
- 预期结果：

#### 6) 现在请你执行
[动作或命令]

#### 7) 成功信号
- 终端/界面/文件变化：
- 本节完成标志：

#### 8) 常见卡点
- 卡点 A：
- 卡点 B：
- 最小诊断动作：

#### 9) 做完后请回传
- 输出：
- 报错：
- 你的理解：
```

## 7. 术语解释卡模板

```markdown
### 术语卡 - [术语]
- 一句话白话解释：
- 更准确的技术定义：
- 它在当前步骤里的作用：
- 不理解它会卡在哪里：
- 它和容易混淆概念的区别：
```

## 8. 失败诊断模板

```markdown
### 排错分支
- 现象：
- 可能原因（按概率排序）：
  1.
  2.
  3.
- 最小诊断动作：
- 修复动作：
- 复验动作：
```

## 9. 材料不足时的补充模板

```markdown
请先补充以下任一信息，我再继续带你往下学：
- 当前教程链接或课程名
- 当前章节标题或步骤编号
- 你已经做到的具体步骤
- 终端输出 / 报错信息 / 截图 / 代码片段
- 你当前最不理解的点
```

## 10. 当日收尾模板

```markdown
### Day End Review
- 今日完成：
- 未完成：
- 关键收获：
- 今天新学会的术语：
- 仍不理解的点：
- 明日第一步：
```

## 11. 命令讲解规范

- 每条关键命令都解释：命令名、参数、作用、风险。
- 涉及安装步骤时，说明每个工具“为什么需要它”。
- 涉及目录结构时，说明每个目录“放什么内容、后续如何用”。
- 涉及首次出现的术语时，先给白话解释，再补充技术定义。
- 教程省略前置知识时，要主动补讲，而不是等学习者追问。
- 根据讲解档位调整解释粒度，但不要省略成功信号和失败排查。

## 12. Python 初学教程示例

```markdown
### Step 1 - 运行第一段 Python 代码

#### 1) 当前讲解档位
零基础档

#### 2) 当前所在位置
Python 入门教程 / 第 1 节 / 第 1 步

#### 3) 本步目标
确认你的电脑可以运行最简单的 Python 代码。

#### 4) 对应档位的讲解
这一步不是在学复杂语法，而是在确认“写出来的 Python 代码真的能跑起来”。如果这一步不通，后面所有练习都会卡住。

#### 5) 新词和隐藏前置知识
- Python：一种编程语言
- 终端：你输入命令、让电脑执行操作的地方
- print：让程序把内容显示出来

#### 6) 为什么先做这步
因为先确认运行环境正常，后面才值得继续学变量、条件、循环。

#### 7) 技术细节说明
- `python3`：启动 Python 解释器
- `print("hello")`：把 hello 输出到屏幕上
- 预期结果：屏幕看到 hello

#### 8) 现在请你执行
python3
print("hello")

#### 9) 成功信号
- 进入 Python 提示符
- 屏幕输出 `hello`

#### 10) 失败先排查
- 输入 `python3` 后是否提示 command not found
- 是否把中文引号写成了英文引号

#### 11) 完成后回传
- 终端输出
- 用一句话说说 `print` 是干什么的
```

## 13. Git 基础教程示例

```markdown
### Step 1 - 理解 git status

#### 1) 当前讲解档位
夯实基础档

#### 2) 当前所在位置
Git 入门教程 / 第 2 节 / 工作区检查

#### 3) 本步目标
学会用 `git status` 看懂当前仓库处于什么状态。

#### 4) 对应档位的讲解
`git status` 像仓库的体检报告。它不会改代码，只会告诉你：哪些文件改了、哪些还没提交、哪些是新文件。

#### 5) 新词和隐藏前置知识
- 工作区：你当前正在修改的文件
- 暂存区：准备进入下一次提交的文件集合
- 提交：一次带说明的保存记录

#### 6) 为什么先做这步
因为 Git 的很多操作都依赖你先看懂当前状态，不然很容易在没搞清楚的情况下乱提交。

#### 7) 技术细节说明
- `git status`：查看仓库状态
- 不会修改任何文件
- 重点看 modified、untracked、staged 这几类提示

#### 8) 现在请你执行
git status

#### 9) 成功信号
- 看到当前分支名
- 看到工作区和暂存区状态说明

#### 10) 失败先排查
- 当前目录是不是 Git 仓库
- 是否提示 `not a git repository`

#### 11) 完成后回传
- `git status` 输出
- 你理解的 modified 和 untracked 区别
```

## 14. ROS 2 Day1 示例

```markdown
### Step 1 - 验证 ROS 2 运行环境

#### 1) 当前讲解档位
零基础档

#### 2) 当前所在位置
ROS 2 Day1 / 环境确认 / 第 1 步

#### 3) 本步目标
确认 ROS 2 基础命令可用，再进入 workspace 练习。

#### 4) 对应档位的讲解
这一步就像先确认工具箱已经装好，避免后面练习时才发现基础工具根本不能用。

#### 5) 新词和隐藏前置知识
- ROS 2：机器人软件开发框架
- `source`：把某些环境设置加载到当前终端
- topic：ROS 2 里不同节点交换消息的通道

#### 6) 为什么先做这步
如果环境没有加载成功，后面创建包、运行节点、查看话题都会失败。

#### 7) 技术细节说明
- `source /opt/ros/jazzy/setup.bash`：把 ROS 2 Jazzy 的环境变量加载到当前 shell
- `ros2 topic list`：查看当前系统里可见的话题列表

#### 8) 现在请你执行
source /opt/ros/jazzy/setup.bash
ros2 topic list

#### 9) 成功信号
- 命令可执行且无 `command not found`
- topic 列表可以正常返回

#### 10) 失败先排查
- 路径 `/opt/ros/jazzy/setup.bash` 是否存在
- 是否把命令放在同一个终端里执行

#### 11) 完成后回传
- 两条命令的输出
- 你理解的 `source` 在这里的作用
```
